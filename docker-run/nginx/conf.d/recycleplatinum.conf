server {
  listen 443;
  server_name *.recycleplatinum.com;
  ssl_certificate      /etc/ssl/recycleplatinum.cer;
  ssl_certificate_key /etc/ssl/recycleplatinum.key;

  access_log /var/log/nginx/recycleplatinum-access.log;
  error_log /var/log/nginx/recycleplatinum-error.log;

  location / {
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   Host      $http_host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_pass http://drupal_nginx;
  }
}
