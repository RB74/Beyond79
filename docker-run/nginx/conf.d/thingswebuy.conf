server {
  listen 443;
  server_name *.thingswebuy.com;
  ssl_certificate      /etc/ssl/thingswebuy.cer;
  ssl_certificate_key /etc/ssl/thingswebuy.key;

  access_log /var/log/nginx/thingswebuy-access.log;
  error_log /var/log/nginx/thingswebuy-error.log;

  location / {
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_set_header   Host      $http_host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_pass http://drupal_nginx;
  }
}
